<%- model_class = Sensor -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:ref) %></th>
      <th><%= model_class.human_attribute_name(:state) %></th>
      <th><%= model_class.human_attribute_name(:sensor_type_id) %></th>
      <th><%= model_class.human_attribute_name(:green_kub_box_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @sensors.each do |sensor| %>
      <tr>
        <td><%= link_to sensor.id, sensor_path(sensor) %></td>
        <td><%= sensor.ref %></td>
        <td><%= sensor.state %></td>
        <td><%= sensor.sensor_type_id %></td>
        <td><%= sensor.green_kub_box_id %></td>
        <td><%=l sensor.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_sensor_path(sensor), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      sensor_path(sensor),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_sensor_path,
            :class => 'btn btn-primary' %>
